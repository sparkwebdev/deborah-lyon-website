<!-- Hero -->
<section class="hero">
    <div class="container">
        <div class="hero__content">
            <p class="hero__quote">{{ hero.quote }}</p>
            <a href="{{ site.bookingUrl }}" class="btn" target="_blank" rel="noopener noreferrer">{{ hero.buttonText }}</a>
        </div>
    </div>
</section>
