<!-- Gift Panel -->
<section class="gift-panel bg-brand">
    <div class="container gift-panel__content">
        <img src="{{ giftPanel.icon }}" alt="" class="gift-panel__icon">
        <p class="gift-panel__text">{{ giftPanel.text }}</p>
        <a href="{{ site.bookingUrl }}" class="btn btn--inverted btn--small" target="_blank" rel="noopener noreferrer">{{ giftPanel.buttonText }}</a>
    </div>
</section>
