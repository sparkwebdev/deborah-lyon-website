<!-- Book Now -->
<section class="book-now bg-brand">
    <div class="container">
        <h2>{{ bookNow.heading }}</h2>
        <p class="text-balance">{{ bookNow.description }}</p>
        <a href="{{ site.bookingUrl }}" class="btn btn--inverted" target="_blank" rel="noopener noreferrer">{{ bookNow.buttonText }}</a>
    </div>
</section>
